- graphQL:
    method: post
  javascriptFile: addToCart.js
  
- graphQL:
    method get
  javascript: |
    function response(request, cache) {
      return Response.ok(Body.json(cache.cart ?? {}));
    }
